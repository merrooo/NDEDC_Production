The Best Solution for avoid the problem of commit:-

(> git push -u Desktop-Driver main
To https://github.com/merrooo/Desktop-Driver.git
 ! [rejected]        main -> main (non-fast-forward)
error: failed to push some refs to 'https://github.com/merrooo/Desktop-Driver.git'
hint: Updates were rejected because the tip of your current branch is behind
hint: its remote counterpart. If you want to integrate the remote changes,
hint: use 'git pull' before pushing again.
hint: See the 'Note about fast-forwards' in 'git push --help' for details.
)
**
git pull Desktop-Driver main --allow-unrelated-histories
git push -u Desktop-Driver main --force

---------------------------------------------------------------------------

git --version
git init
git status

* upload your files to github 

1-right click on VSC and open new file
2-Clone Git Repository
3-Choose your folder in github
4-Then select the location in PC that can Download in
5-Edite your file html and finish it
6-Then Open logo in VSC (Source Control for github) he will asks for the files you change it To Commit


----------------------------------------------


Step 1: Install Git (IMPORTANT OPTIONS)

During installation:

✔ Click Next until you see
“Adjusting your PATH environment”

👉 Select THIS option (VERY IMPORTANT):

✅ Git from the command line and also from 3rd-party software

Then:

Keep default options

Finish install

🔹 Step 3: RESTART EVERYTHING

This is NOT optional.

🔁 Restart:

VS Code

PowerShell / CMD

(Safest) Restart your PC

🔹 Step 4: Verify Git is installed

Open VS Code → Terminal and run:

git --version


✅ You should see something like:

git version 2.44.0.windows.1

git add.
git commit -am "Anything"
git push
git status

echo "# NDEDC_Smart_Meter" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/merrooo/NDEDC_Smart_Meter.git
git push -u origin main

git remote add origin https://github.com/merrooo/NDEDC_Smart_Meter.git
git branch -M main
git push -u origin main

---------------------------------------------------------------------------
for commit in VSC
open terminal and type this two codes

git config --global user.name "merrooo"
git config --global user.email "amirmerrooo@gmail.com"



